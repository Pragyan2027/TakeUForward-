class Solution {
    public void rotateArrayByOne(int[] nums) {
        for(int i = 0 ; i < nums.length - 1 ; i++){
            swap(i , nums);
        }
    }
    public void swap(int i , int[] nums){
        int temp = nums[i];
        nums[i] = nums[i+1];
        nums[i+1] = temp;
    }
}
